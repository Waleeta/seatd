module SearchHelper

# def find_businesses(service)
#   @services = Service.where(service_type: service)
#   business_ids = @services.map {|service| service.business_id }
#   found_businesses = business_ids.map {|id| Business.find(id) }
# end

end
